@model IEnumerable<WebShoppingOnline.Models.ApplicationUser>
@{
    ViewBag.Title = "Quản lí tài khoản";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}
@section naviheader{
    <!-- Left navbar links -->
    <ul class="navbar-nav">
        <li class="nav-item">
            <a class="nav-link" data-widget="pushmenu" href="#" role="button"><i class="fas fa-bars"></i></a>
        </li>
        <li class="nav-item d-none d-sm-inline-block">
            <a href="/Admin/Home" class="nav-link">Trang chủ</a>
        </li>
        <li class="nav-item d-none d-sm-inline-block">
            <a href="/Admin/Role" class="nav-link">@ViewBag.Title</a>
        </li>
    </ul>
}
<!-- Content Header (Page header) -->
<section class="content-header">
    <div class="container-fluid">
        <div class="row mb-2">
            <div class="col-sm-6">
                <h1>@ViewBag.Title</h1>
            </div>
            <div class="col-sm-6">
                <ol class="breadcrumb float-sm-right">
                    <li class="breadcrumb-item"><a href="/Admin/Home">Trang chủ</a></li>
                    <li class="breadcrumb-item active">Manage-Account</li>
                </ol>
            </div>
        </div>
    </div><!-- /.container-fluid -->
</section>

<!-- Main content -->
<section class="content">

    <!-- Default box -->
    <div class="card">
        <div class="card-header">
            <h3 class="card-title">@ViewBag.Title</h3>

            <div class="card-tools">
                <a href="/admin/account/create" class="btn btn-sm btn-success">
                    Tạo mới tài khoản
                </a>
            </div>
        </div>
        <div class="card-body">
            <table class="table table-bordered table-hover">
                <thead>
                    <tr>
                        <th>#</th>
                        <th>Tài khoản</th>
                        <th>Họ tên</th>
                        <th>Email</th>
                        <th>Phone</th>
                        <th style="text-align:center">Thao tác</th>
                    </tr>
                </thead>
                <tbody>
                    @if (Model != null && Model.Any())
                    {
                        var i = 1;
                        foreach (var item1 in Model)
                        {
                            <tr>
                                <td>@(i++)</td>
                                <td>@item1.UserName</td>
                                <td>@item1.FullName</td>
                                <td>@item1.Email</td>
                                <td>@item1.Phone</td>
                                <td style="text-align:center">
                                    <a href="/admin/account/edit/@item1.Id" class="btn btn-sm btn-success">
                                        <i class="fa-book-reader"> Cập nhật</i>
                                    </a>
                                    <a href="#" class="btn btn-sm btn-danger btnXoaAccount" data-user="@item1.UserName">
                                        <i class="fa fa-trash"> Xóa </i>
                                    </a>
                                </td>
                            </tr>
                        }
                    }
                    else
                    {
                <tr>
                    <td style="text-align:left; color:red" colspan="6">Chưa có tài khoản nào trong hệ thống</td>
                </tr>
                    }
                </tbody>

            </table>

        </div>
        <!-- /.card-body -->
        <div class="card-footer">
        </div>
        <!-- /.card-footer-->
    </div>
    <!-- /.card -->

</section>
<!-- /.content -->
